[project]
name = "tiny-crm"
version = "0.1.0"
description = "Tiny CRM — starter app for Introduction to Programming, Nova SBE 2026/27"
requires-python = ">=3.12,<3.13"
dependencies = [
    "streamlit==1.62.0",
    "pytest==9.1.1",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
